.DS_Store
Thumbs.db

# Segreti: mai in git, restano solo nel backup zip su Dropbox (backup-config.ps1)
.env

# Installer vendor statici (~69MB): non beneficiano di versionamento,
# recuperabili dal sito Oracle o dal backup zip Dropbox
oracle/*.zip

# Backup/dump non vanno versionati qui (vivono in Dropbox BCK)
*.sql
